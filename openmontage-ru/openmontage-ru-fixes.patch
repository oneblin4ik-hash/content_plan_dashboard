diff --git a/remotion-composer/src/CinematicRenderer.tsx b/remotion-composer/src/CinematicRenderer.tsx
index 926c59c..0bfac77 100644
--- a/remotion-composer/src/CinematicRenderer.tsx
+++ b/remotion-composer/src/CinematicRenderer.tsx
@@ -1,5 +1,5 @@
+import { displayFamily } from "./lib/cyrillicFonts";
 import React from "react";
-import { loadFont } from "@remotion/google-fonts/SpaceGrotesk";
 import {
   AbsoluteFill,
   Audio,
@@ -18,10 +18,7 @@ import { resolveAsset } from "./lib/resolveAsset";
 
 const FPS = 30;
 
-const { fontFamily } = loadFont("normal", {
-  weights: ["400", "500", "700"],
-  subsets: ["latin"],
-});
+const fontFamily = displayFamily;
 
 const toneGradient = (tone: CinematicTone) => {
   switch (tone) {
diff --git a/remotion-composer/src/CollageBurst.tsx b/remotion-composer/src/CollageBurst.tsx
index 77cf0c7..31e0248 100644
--- a/remotion-composer/src/CollageBurst.tsx
+++ b/remotion-composer/src/CollageBurst.tsx
@@ -1,3 +1,4 @@
+import { serifFamily } from "./lib/cyrillicFonts";
 import {
   AbsoluteFill,
   Img,
@@ -10,17 +11,10 @@ import {
   useVideoConfig,
 } from "remotion";
 import React from "react";
-import { loadFont as loadPlayfair } from "@remotion/google-fonts/PlayfairDisplay";
 import { resolveAsset } from "./lib/resolveAsset";
 
-const { fontFamily: playfairFamily } = loadPlayfair("normal", {
-  weights: ["400", "700"],
-  subsets: ["latin"],
-});
-const { fontFamily: playfairItalic } = loadPlayfair("italic", {
-  weights: ["400", "700"],
-  subsets: ["latin"],
-});
+const playfairFamily = serifFamily;
+const playfairItalic = serifFamily;
 
 export type CollageTransition =
   | "pop"
diff --git a/remotion-composer/src/Explainer.tsx b/remotion-composer/src/Explainer.tsx
index 99dd637..d5e47e6 100644
--- a/remotion-composer/src/Explainer.tsx
+++ b/remotion-composer/src/Explainer.tsx
@@ -1,3 +1,4 @@
+import { displayFamily } from "./lib/cyrillicFonts";
 import {
   AbsoluteFill,
   Audio,
@@ -9,7 +10,6 @@ import {
   useCurrentFrame,
   useVideoConfig,
 } from "remotion";
-import { loadFont } from "@remotion/google-fonts/SpaceGrotesk";
 import { TextCard } from "./components/TextCard";
 import { StatCard } from "./components/StatCard";
 import { CalloutBox } from "./components/CalloutBox";
@@ -35,10 +35,7 @@ import type { ParticleType } from "./components/ParticleOverlay";
 import { resolveTheme, type ThemeConfig, DEFAULT_THEME } from "./Root";
 
 // Load Space Grotesk font for cinematic typography
-const { fontFamily } = loadFont("normal", {
-  weights: ["400", "700"],
-  subsets: ["latin"],
-});
+const fontFamily = displayFamily;
 
 // ---------------------------------------------------------------------------
 // Animated Background — Gradient Mesh + Floating Orbs
diff --git a/remotion-composer/src/LyricOverlay.tsx b/remotion-composer/src/LyricOverlay.tsx
index 287110f..e9fedb2 100644
--- a/remotion-composer/src/LyricOverlay.tsx
+++ b/remotion-composer/src/LyricOverlay.tsx
@@ -1,3 +1,4 @@
+import { serifFamily } from "./lib/cyrillicFonts";
 import {
   AbsoluteFill,
   Audio,
@@ -7,13 +8,9 @@ import {
   useVideoConfig,
 } from "remotion";
 import React from "react";
-import { loadFont as loadPlayfair } from "@remotion/google-fonts/PlayfairDisplay";
 import { resolveAsset } from "./lib/resolveAsset";
 
-const { fontFamily: playfairItalic } = loadPlayfair("italic", {
-  weights: ["400", "700"],
-  subsets: ["latin"],
-});
+const playfairItalic = serifFamily;
 
 export interface Lyric {
   text: string;
diff --git a/remotion-composer/src/Root.tsx b/remotion-composer/src/Root.tsx
index d837f14..d614102 100644
--- a/remotion-composer/src/Root.tsx
+++ b/remotion-composer/src/Root.tsx
@@ -46,8 +46,8 @@ export const THEMES: Record<string, ThemeConfig> = {
     surfaceColor: "#F9FAFB",
     textColor: "#1F2937",
     mutedTextColor: "#6B7280",
-    headingFont: "Inter",
-    bodyFont: "Inter",
+    headingFont: "Inter, 'OM Text', system-ui, sans-serif",
+    bodyFont: "Inter, 'OM Text', system-ui, sans-serif",
     monoFont: "JetBrains Mono",
     chartColors: ["#2563EB", "#F59E0B", "#10B981", "#8B5CF6", "#EC4899", "#06B6D4"],
     springConfig: { damping: 20, stiffness: 120, mass: 1 },
@@ -62,8 +62,8 @@ export const THEMES: Record<string, ThemeConfig> = {
     surfaceColor: "#1E293B",
     textColor: "#F8FAFC",
     mutedTextColor: "#94A3B8",
-    headingFont: "Space Grotesk",
-    bodyFont: "Space Grotesk",
+    headingFont: "'OM Display', Montserrat, 'Space Grotesk', system-ui, sans-serif",
+    bodyFont: "'OM Display', Montserrat, 'Space Grotesk', system-ui, sans-serif",
     monoFont: "Fira Code",
     chartColors: ["#7C3AED", "#EC4899", "#06B6D4", "#F59E0B", "#10B981", "#EF4444"],
     springConfig: { damping: 12, stiffness: 80, mass: 1 },
@@ -78,8 +78,8 @@ export const THEMES: Record<string, ThemeConfig> = {
     surfaceColor: "#FFFFFF",
     textColor: "#1A1A2E",
     mutedTextColor: "#6B7280",
-    headingFont: "IBM Plex Sans",
-    bodyFont: "IBM Plex Sans",
+    headingFont: "IBM Plex Sans, 'OM Text', system-ui, sans-serif",
+    bodyFont: "IBM Plex Sans, 'OM Text', system-ui, sans-serif",
     monoFont: "IBM Plex Mono",
     chartColors: ["#E94560", "#1A1A2E", "#0F3460", "#9CA3AF"],
     springConfig: { damping: 25, stiffness: 150, mass: 1 },
diff --git a/remotion-composer/src/TalkingHead.tsx b/remotion-composer/src/TalkingHead.tsx
index 6f3b63c..f5ad0fd 100644
--- a/remotion-composer/src/TalkingHead.tsx
+++ b/remotion-composer/src/TalkingHead.tsx
@@ -17,6 +17,7 @@ import { LineChart } from "./components/charts/LineChart";
 import { PieChart } from "./components/charts/PieChart";
 import { KPIGrid } from "./components/charts/KPIGrid";
 import { HeroTitle } from "./components/HeroTitle";
+import { HookTitle, HookTitleProps } from "./components/HookTitle";
 import { SectionTitle } from "./components/SectionTitle";
 import { StatReveal } from "./components/StatReveal";
 
@@ -308,6 +309,13 @@ export interface TalkingHeadProps {
   captionFontFamily?: string;
   // Pass "" for CJK captions (no inter-word spacing); defaults to " ".
   captionWordSeparator?: string;
+  captionVerticalPosition?: number;
+  captionTextTransform?: "none" | "uppercase" | "lowercase";
+  captionTextShadow?: string;
+  captionDimUpcoming?: boolean;
+  captionInstantIn?: boolean;
+  /** Opening hook card that builds line by line, with its own timing. */
+  hookTitle?: HookTitleProps & { in_seconds?: number; out_seconds?: number };
 }
 
 export const TalkingHead: React.FC<TalkingHeadProps> = ({
@@ -321,6 +329,12 @@ export const TalkingHead: React.FC<TalkingHeadProps> = ({
   captionBackgroundColor = "rgba(0, 0, 0, 0.65)",
   captionFontFamily,
   captionWordSeparator,
+  captionVerticalPosition,
+  captionTextTransform,
+  captionTextShadow,
+  captionDimUpcoming,
+  captionInstantIn,
+  hookTitle,
 }) => {
   const { fps } = useVideoConfig();
 
@@ -349,6 +363,21 @@ export const TalkingHead: React.FC<TalkingHeadProps> = ({
         );
       })}
 
+      {/* Layer 2b: Opening hook card */}
+      {hookTitle ? (
+        <Sequence
+          from={Math.round((hookTitle.in_seconds ?? 0) * fps)}
+          durationInFrames={Math.max(
+            1,
+            Math.round(
+              ((hookTitle.out_seconds ?? 4) - (hookTitle.in_seconds ?? 0)) * fps
+            )
+          )}
+        >
+          <HookTitle {...hookTitle} />
+        </Sequence>
+      ) : null}
+
       {/* Layer 3: Captions (topmost — always visible above overlays) */}
       <CaptionOverlay
         words={captions}
@@ -359,6 +388,13 @@ export const TalkingHead: React.FC<TalkingHeadProps> = ({
         color={captionColor}
         {...(captionFontFamily ? { fontFamily: captionFontFamily } : {})}
         {...(captionWordSeparator !== undefined ? { wordSeparator: captionWordSeparator } : {})}
+        {...(captionVerticalPosition !== undefined
+          ? { verticalPosition: captionVerticalPosition }
+          : {})}
+        {...(captionTextTransform ? { textTransform: captionTextTransform } : {})}
+        {...(captionTextShadow !== undefined ? { textShadow: captionTextShadow } : {})}
+        {...(captionDimUpcoming !== undefined ? { dimUpcoming: captionDimUpcoming } : {})}
+        {...(captionInstantIn !== undefined ? { instantIn: captionInstantIn } : {})}
       />
     </AbsoluteFill>
   );
diff --git a/remotion-composer/src/TitledVideo.tsx b/remotion-composer/src/TitledVideo.tsx
index 0f710ca..f6e33f8 100644
--- a/remotion-composer/src/TitledVideo.tsx
+++ b/remotion-composer/src/TitledVideo.tsx
@@ -1,3 +1,4 @@
+import { serifFamily } from "./lib/cyrillicFonts";
 import {
   AbsoluteFill,
   CalculateMetadataFunction,
@@ -9,15 +10,11 @@ import {
   useVideoConfig,
 } from "remotion";
 import { getVideoMetadata } from "@remotion/media-utils";
-import { loadFont } from "@remotion/google-fonts/PlayfairDisplay";
 import { resolveAsset } from "./lib/resolveAsset";
 
 // Editorial serif for the tagline — Playfair Display at its boldest weight.
 // Loaded once at module scope so every render reuses the same font face.
-const { fontFamily } = loadFont("normal", {
-  weights: ["400", "700", "900"],
-  subsets: ["latin"],
-});
+const fontFamily = serifFamily;
 
 export type TitledVideoProps = {
   videoSrc: string;
diff --git a/remotion-composer/src/components/CalloutBox.tsx b/remotion-composer/src/components/CalloutBox.tsx
index 1609739..12503d6 100644
--- a/remotion-composer/src/components/CalloutBox.tsx
+++ b/remotion-composer/src/components/CalloutBox.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -40,7 +41,7 @@ export const CalloutBox: React.FC<CalloutBoxProps> = ({
   borderColor,
   backgroundColor,
   textColor = "#1F2937",
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   fontSize = 32,
   titleFontSize = 38,
   containerBackgroundColor = "#FFFFFF",
diff --git a/remotion-composer/src/components/CaptionOverlay.tsx b/remotion-composer/src/components/CaptionOverlay.tsx
index 0142d68..6e1e70f 100644
--- a/remotion-composer/src/components/CaptionOverlay.tsx
+++ b/remotion-composer/src/components/CaptionOverlay.tsx
@@ -1,3 +1,5 @@
+import React from "react";
+import { displayFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   Sequence,
@@ -29,6 +31,22 @@ type CaptionOverlayProps = {
   // Separator rendered between words. Space-delimited languages want the
   // default " "; CJK languages (no inter-word spacing) should pass "".
   wordSeparator?: string;
+  // Where the caption sits vertically, as a fraction of frame height for the
+  // block's centre (0 = top, 1 = bottom). Omit to keep the bottom-anchored
+  // default. Short-form styles often park captions around 0.6.
+  verticalPosition?: number;
+  // Applied to every word — "uppercase" matches the all-caps look common in
+  // short-form captions.
+  textTransform?: "none" | "uppercase" | "lowercase";
+  // Shadow behind the text. Pass "" to drop it.
+  textShadow?: string;
+  // Words that have not started yet are dimmed by default. One-word-per-page
+  // styles want every word at full strength.
+  dimUpcoming?: boolean;
+  // Skip the spring entrance. A one-word page can be on screen for under ten
+  // frames — shorter than the spring takes to settle — so the word would
+  // never reach full opacity. Short-form captions want it to just appear.
+  instantIn?: boolean;
 };
 
 interface CaptionPage {
@@ -65,31 +83,67 @@ const PageRenderer: React.FC<{
   backgroundColor: string;
   fontFamily: string;
   wordSeparator: string;
-}> = ({ page, fontSize, color, highlightColor, backgroundColor, fontFamily, wordSeparator }) => {
+  verticalPosition?: number;
+  textTransform: "none" | "uppercase" | "lowercase";
+  textShadow: string;
+  dimUpcoming: boolean;
+  instantIn: boolean;
+}> = ({
+  page,
+  fontSize,
+  color,
+  highlightColor,
+  backgroundColor,
+  fontFamily,
+  wordSeparator,
+  verticalPosition,
+  textTransform,
+  textShadow,
+  dimUpcoming,
+  instantIn,
+}) => {
   const frame = useCurrentFrame();
   const { fps } = useVideoConfig();
 
   const currentMs = page.startMs + (frame / fps) * 1000;
 
   // Spring entrance
-  const entrance = spring({
+  const spring_ = spring({
     frame,
     fps,
     config: { damping: 18, stiffness: 120 },
   });
+  const entrance = instantIn ? 1 : spring_;
 
   return (
     <AbsoluteFill
       style={{
         justifyContent: "flex-end",
         alignItems: "center",
-        paddingBottom: 80,
+        paddingBottom: verticalPosition === undefined ? 80 : 0,
       }}
     >
       <div
         style={{
+          // AbsoluteFill is a full-height box, so offsetting it with `top`
+          // and a percentage translate lands in the wrong place. Position the
+          // caption block itself instead: its own height is what the -50%
+          // translate needs to work against. Both translates live in one
+          // string — a second `transform` key would silently replace the
+          // first and drop the centring.
+          ...(verticalPosition === undefined
+            ? {}
+            : {
+                position: "absolute" as const,
+                top: `${verticalPosition * 100}%`,
+              }),
           opacity: entrance,
-          transform: `translateY(${interpolate(entrance, [0, 1], [20, 0])}px)`,
+          transform: [
+            verticalPosition === undefined ? null : "translateY(-50%)",
+            `translateY(${interpolate(entrance, [0, 1], [20, 0])}px)`,
+          ]
+            .filter(Boolean)
+            .join(" "),
           backgroundColor,
           borderRadius: 12,
           padding: "14px 28px",
@@ -110,23 +164,35 @@ const PageRenderer: React.FC<{
             const isActive = w.startMs <= currentMs && w.endMs > currentMs;
             const isPast = w.endMs <= currentMs;
             return (
-              <span
-                key={`${w.startMs}-${i}`}
-                style={{
-                  // Keep each word unbroken so lines wrap only at word
-                  // boundaries. For space-delimited text this matches the
-                  // previous behavior; for CJK it prevents mid-word breaks.
-                  display: "inline-block",
-                  whiteSpace: "nowrap",
-                  color: isActive ? highlightColor : isPast ? color : `${color}99`,
-                  transition: "none", // CSS transitions forbidden in Remotion
-                  textShadow: isActive
-                    ? `0 0 20px ${highlightColor}66, 0 2px 4px rgba(0,0,0,0.5)`
-                    : "0 2px 4px rgba(0,0,0,0.5)",
-                }}
-              >
-                {w.word}{i < page.words.length - 1 ? wordSeparator : ""}
-              </span>
+              // The separator sits OUTSIDE the word span on purpose. Inside an
+              // inline-block with white-space: nowrap a trailing space is
+              // trimmed by the browser, which glues adjacent words together.
+              // The parent span carries white-space: pre-wrap, so a separator
+              // placed here survives.
+              <React.Fragment key={`${w.startMs}-${i}`}>
+                <span
+                  style={{
+                    // Keep each word unbroken so lines wrap only at word
+                    // boundaries. For space-delimited text this matches the
+                    // previous behavior; for CJK it prevents mid-word breaks.
+                    display: "inline-block",
+                    whiteSpace: "nowrap",
+                    color: isActive
+                      ? highlightColor
+                      : isPast || !dimUpcoming
+                        ? color
+                        : `${color}99`,
+                    transition: "none", // CSS transitions forbidden in Remotion
+                    textTransform,
+                    textShadow: isActive
+                      ? `0 0 20px ${highlightColor}66, ${textShadow}`
+                      : textShadow,
+                  }}
+                >
+                  {w.word}
+                </span>
+                {i < page.words.length - 1 ? wordSeparator : ""}
+              </React.Fragment>
             );
           })}
         </span>
@@ -142,8 +208,13 @@ export const CaptionOverlay: React.FC<CaptionOverlayProps> = ({
   color = "#F8FAFC",
   highlightColor = "#22D3EE",
   backgroundColor = "rgba(15, 23, 42, 0.75)",
-  fontFamily = "Space Grotesk, Inter, system-ui, sans-serif",
+  fontFamily = displayFamily,
   wordSeparator = " ",
+  verticalPosition,
+  textTransform = "none",
+  textShadow = "0 2px 4px rgba(0,0,0,0.5)",
+  dimUpcoming = true,
+  instantIn = false,
 }) => {
   const { fps } = useVideoConfig();
   const pages = buildPages(words, wordsPerPage);
@@ -168,6 +239,11 @@ export const CaptionOverlay: React.FC<CaptionOverlayProps> = ({
               backgroundColor={backgroundColor}
               fontFamily={fontFamily}
               wordSeparator={wordSeparator}
+              verticalPosition={verticalPosition}
+              textTransform={textTransform}
+              textShadow={textShadow}
+              dimUpcoming={dimUpcoming}
+              instantIn={instantIn}
             />
           </Sequence>
         );
diff --git a/remotion-composer/src/components/ComparisonCard.tsx b/remotion-composer/src/components/ComparisonCard.tsx
index 0ec43ef..ee9ffec 100644
--- a/remotion-composer/src/components/ComparisonCard.tsx
+++ b/remotion-composer/src/components/ComparisonCard.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -40,7 +41,7 @@ export const ComparisonCard: React.FC<ComparisonCardProps> = ({
   backgroundColor = "#FFFFFF",
   cardBackgroundColor = "#F3F4F6",
   textColor = "#1F2937",
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   titleFontSize = 44,
   labelFontSize = 28,
   valueFontSize = 72,
diff --git a/remotion-composer/src/components/EndTag.tsx b/remotion-composer/src/components/EndTag.tsx
index ab785c5..5e45992 100644
--- a/remotion-composer/src/components/EndTag.tsx
+++ b/remotion-composer/src/components/EndTag.tsx
@@ -1,3 +1,4 @@
+import { displayFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -137,8 +138,7 @@ export const EndTag: React.FC<EndTagProps> = ({
         {/* The tag line */}
         <div
           style={{
-            fontFamily:
-              "'Space Grotesk', 'Inter', 'Helvetica Neue', system-ui, sans-serif",
+            fontFamily: displayFamily,
             fontWeight: 900,
             fontSize: 84,
             letterSpacing: "0.12em",
diff --git a/remotion-composer/src/components/HeroTitle.tsx b/remotion-composer/src/components/HeroTitle.tsx
index e22e4bd..82660d3 100644
--- a/remotion-composer/src/components/HeroTitle.tsx
+++ b/remotion-composer/src/components/HeroTitle.tsx
@@ -1,3 +1,4 @@
+import { displayFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -54,7 +55,7 @@ export const HeroTitle: React.FC<HeroTitleProps> = ({
           style={{
             fontSize: 72,
             fontWeight: 800,
-            fontFamily: "Space Grotesk, Inter, system-ui, sans-serif",
+            fontFamily: displayFamily,
             lineHeight: 1.2,
             display: "flex",
             justifyContent: "center",
@@ -101,7 +102,7 @@ export const HeroTitle: React.FC<HeroTitleProps> = ({
               fontSize: 28,
               fontWeight: 400,
               color: subtitleColor,
-              fontFamily: "Space Grotesk, Inter, system-ui, sans-serif",
+              fontFamily: displayFamily,
               letterSpacing: "0.1em",
               textTransform: "uppercase",
             }}
diff --git a/remotion-composer/src/components/ProgressBar.tsx b/remotion-composer/src/components/ProgressBar.tsx
index 30e6a78..5f690f1 100644
--- a/remotion-composer/src/components/ProgressBar.tsx
+++ b/remotion-composer/src/components/ProgressBar.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -42,7 +43,7 @@ export const ProgressBar: React.FC<ProgressBarProps> = ({
   segments,
   height = 32,
   borderRadius = 8,
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   textColor = "#1F2937",
   labelFontSize = 36,
   percentageFontSize = 28,
diff --git a/remotion-composer/src/components/ProviderChip.tsx b/remotion-composer/src/components/ProviderChip.tsx
index 0f11230..85f270e 100644
--- a/remotion-composer/src/components/ProviderChip.tsx
+++ b/remotion-composer/src/components/ProviderChip.tsx
@@ -1,3 +1,4 @@
+import { displayFamily, textFamily } from "../lib/cyrillicFonts";
 import { AbsoluteFill, interpolate, spring, useCurrentFrame, useVideoConfig } from "remotion";
 
 /**
@@ -70,7 +71,7 @@ export const ProviderChip: React.FC<ProviderChipProps> = ({
           style={{
             fontSize: 16,
             color: "rgba(255,255,255,0.6)",
-            fontFamily: "Inter, sans-serif",
+            fontFamily: textFamily,
             fontWeight: 500,
             letterSpacing: 1.5,
             textTransform: "uppercase",
@@ -85,7 +86,7 @@ export const ProviderChip: React.FC<ProviderChipProps> = ({
             border: `2px solid ${accentColor}`,
             borderRadius: 999,
             color: accentColor,
-            fontFamily: "'Space Grotesk', Inter, sans-serif",
+            fontFamily: displayFamily,
             fontWeight: 700,
             fontSize: 28,
             letterSpacing: 0.3,
diff --git a/remotion-composer/src/components/ScreenshotScene.tsx b/remotion-composer/src/components/ScreenshotScene.tsx
index b1a2407..d04f096 100644
--- a/remotion-composer/src/components/ScreenshotScene.tsx
+++ b/remotion-composer/src/components/ScreenshotScene.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   Img,
@@ -383,7 +384,7 @@ const OverlayForStep: React.FC<OverlayProps> = ({
           display: "flex",
           alignItems: "center",
           paddingLeft: Math.round(rect.w * 0.012),
-          fontFamily: "Inter, -apple-system, sans-serif",
+          fontFamily: textFamily,
           fontSize: fontPx,
           color: step.color ?? "#E5E7EB",
           pointerEvents: "none",
@@ -462,7 +463,7 @@ const OverlayForStep: React.FC<OverlayProps> = ({
           border: `1px solid ${border}`,
           borderRadius: Math.round(rect.w * 0.008),
           padding: `${Math.round(rect.h * 0.015)}px ${Math.round(rect.w * 0.012)}px`,
-          fontFamily: "Inter, -apple-system, sans-serif",
+          fontFamily: textFamily,
           fontSize: fontPx,
           color: "#F1F5F9",
           lineHeight: 1.5,
@@ -635,7 +636,7 @@ const OverlayForStep: React.FC<OverlayProps> = ({
           width: maxW,
           background: color,
           color: "#0B0F1A",
-          fontFamily: "Inter, -apple-system, sans-serif",
+          fontFamily: textFamily,
           fontWeight: 600,
           fontSize: fontPx,
           lineHeight: 1.35,
diff --git a/remotion-composer/src/components/SectionTitle.tsx b/remotion-composer/src/components/SectionTitle.tsx
index c244f2c..4af4b55 100644
--- a/remotion-composer/src/components/SectionTitle.tsx
+++ b/remotion-composer/src/components/SectionTitle.tsx
@@ -1,3 +1,4 @@
+import { displayFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -71,7 +72,7 @@ export const SectionTitle: React.FC<SectionTitleProps> = ({
             fontSize: 28,
             fontWeight: 700,
             color: textColor,
-            fontFamily: "Space Grotesk, Inter, system-ui, sans-serif",
+            fontFamily: displayFamily,
             letterSpacing: "0.05em",
             textTransform: "uppercase",
             textShadow: "0 2px 8px rgba(0,0,0,0.6)",
@@ -85,7 +86,7 @@ export const SectionTitle: React.FC<SectionTitleProps> = ({
               fontSize: 18,
               fontWeight: 400,
               color: accentColor,
-              fontFamily: "Space Grotesk, Inter, system-ui, sans-serif",
+              fontFamily: displayFamily,
               marginTop: 4,
               opacity: spring({
                 frame: frame - 8,
diff --git a/remotion-composer/src/components/StatCard.tsx b/remotion-composer/src/components/StatCard.tsx
index dc228ed..e91124b 100644
--- a/remotion-composer/src/components/StatCard.tsx
+++ b/remotion-composer/src/components/StatCard.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import { AbsoluteFill, spring, useCurrentFrame, useVideoConfig } from "remotion";
 
 interface StatCardProps {
@@ -50,7 +51,7 @@ export const StatCard: React.FC<StatCardProps> = ({
             transform: `scale(${scale})`,
             fontSize: statFontSize,
             color: accentColor,
-            fontFamily: "Inter, system-ui, sans-serif",
+            fontFamily: textFamily,
             fontWeight: 800,
             lineHeight: 1.1,
           }}
@@ -63,7 +64,7 @@ export const StatCard: React.FC<StatCardProps> = ({
               opacity: subtitleOpacity,
               fontSize: subtitleFontSize,
               color,
-              fontFamily: "Inter, system-ui, sans-serif",
+              fontFamily: textFamily,
               fontWeight: 400,
               marginTop: 16,
             }}
diff --git a/remotion-composer/src/components/StatReveal.tsx b/remotion-composer/src/components/StatReveal.tsx
index a1b7b27..dda247d 100644
--- a/remotion-composer/src/components/StatReveal.tsx
+++ b/remotion-composer/src/components/StatReveal.tsx
@@ -1,3 +1,4 @@
+import { displayFamily } from "../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -70,7 +71,7 @@ export const StatReveal: React.FC<StatRevealProps> = ({
             fontSize: 96,
             fontWeight: 800,
             color: accentColor,
-            fontFamily: "Space Grotesk, Inter, system-ui, sans-serif",
+            fontFamily: displayFamily,
             lineHeight: 1,
             textShadow: `0 0 ${interpolate(glow, [0, 1], [0, 30])}px ${accentColor}66, 0 4px 12px rgba(0,0,0,0.5)`,
           }}
@@ -83,7 +84,7 @@ export const StatReveal: React.FC<StatRevealProps> = ({
               fontSize: 22,
               fontWeight: 500,
               color: textColor,
-              fontFamily: "Space Grotesk, Inter, system-ui, sans-serif",
+              fontFamily: displayFamily,
               marginTop: 8,
               opacity: spring({
                 frame: frame - 10,
diff --git a/remotion-composer/src/components/TerminalScene.tsx b/remotion-composer/src/components/TerminalScene.tsx
index 96c8626..2526e33 100644
--- a/remotion-composer/src/components/TerminalScene.tsx
+++ b/remotion-composer/src/components/TerminalScene.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import { AbsoluteFill, interpolate, spring, useCurrentFrame, useVideoConfig } from "remotion";
 
 /**
@@ -150,7 +151,7 @@ export const TerminalScene: React.FC<TerminalSceneProps> = ({
               textAlign: "center",
               color: "#8E8E93",
               fontSize: 16,
-              fontFamily: "Inter, sans-serif",
+              fontFamily: textFamily,
             }}
           >
             {title}
@@ -247,7 +248,7 @@ export const TerminalScene: React.FC<TerminalSceneProps> = ({
                   background: pill.color,
                   color: "#0B0F1A",
                   borderRadius: 999,
-                  fontFamily: "Inter, sans-serif",
+                  fontFamily: textFamily,
                   fontWeight: 700,
                   fontSize: 20,
                   letterSpacing: 0.2,
diff --git a/remotion-composer/src/components/TextCard.tsx b/remotion-composer/src/components/TextCard.tsx
index 6368fe4..55eacca 100644
--- a/remotion-composer/src/components/TextCard.tsx
+++ b/remotion-composer/src/components/TextCard.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../lib/cyrillicFonts";
 import { AbsoluteFill, spring, useCurrentFrame, useVideoConfig } from "remotion";
 
 interface TextCardProps {
@@ -39,7 +40,7 @@ export const TextCard: React.FC<TextCardProps> = ({
           transform: `scale(${scale})`,
           fontSize,
           color,
-          fontFamily: "Inter, system-ui, sans-serif",
+          fontFamily: textFamily,
           fontWeight: 700,
           textAlign: "center",
           maxWidth: "80%",
diff --git a/remotion-composer/src/components/charts/BarChart.tsx b/remotion-composer/src/components/charts/BarChart.tsx
index 386d394..302197b 100644
--- a/remotion-composer/src/components/charts/BarChart.tsx
+++ b/remotion-composer/src/components/charts/BarChart.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -31,7 +32,7 @@ export const BarChart: React.FC<BarChartProps> = ({
   data,
   title,
   colors = ["#2563EB", "#F59E0B", "#10B981", "#EC4899", "#06B6D4", "#8B5CF6"],
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   textColor = "#1F2937",
   backgroundColor = "#FFFFFF",
   gridColor = "#E5E7EB",
diff --git a/remotion-composer/src/components/charts/KPIGrid.tsx b/remotion-composer/src/components/charts/KPIGrid.tsx
index 6c16a7a..bada9ea 100644
--- a/remotion-composer/src/components/charts/KPIGrid.tsx
+++ b/remotion-composer/src/components/charts/KPIGrid.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -36,7 +37,7 @@ export const KPIGrid: React.FC<KPIGridProps> = ({
   title,
   columns = 3,
   colors = ["#2563EB", "#F59E0B", "#10B981", "#EC4899"],
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   textColor = "#1F2937",
   backgroundColor = "#FFFFFF",
   cardBackgroundColor = "#F9FAFB",
diff --git a/remotion-composer/src/components/charts/LineChart.tsx b/remotion-composer/src/components/charts/LineChart.tsx
index 66584a3..1d17386 100644
--- a/remotion-composer/src/components/charts/LineChart.tsx
+++ b/remotion-composer/src/components/charts/LineChart.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -40,7 +41,7 @@ export const LineChart: React.FC<LineChartProps> = ({
   series,
   title,
   colors = ["#2563EB", "#F59E0B", "#10B981", "#EC4899", "#06B6D4", "#8B5CF6"],
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   textColor = "#1F2937",
   backgroundColor = "#FFFFFF",
   gridColor = "#E5E7EB",
diff --git a/remotion-composer/src/components/charts/PieChart.tsx b/remotion-composer/src/components/charts/PieChart.tsx
index 6286388..b666adb 100644
--- a/remotion-composer/src/components/charts/PieChart.tsx
+++ b/remotion-composer/src/components/charts/PieChart.tsx
@@ -1,3 +1,4 @@
+import { textFamily } from "../../lib/cyrillicFonts";
 import {
   AbsoluteFill,
   interpolate,
@@ -32,7 +33,7 @@ export const PieChart: React.FC<PieChartProps> = ({
   data,
   title,
   colors = ["#2563EB", "#F59E0B", "#10B981", "#EC4899", "#06B6D4", "#8B5CF6"],
-  fontFamily = "Inter, system-ui, sans-serif",
+  fontFamily = textFamily,
   textColor = "#1F2937",
   backgroundColor = "#FFFFFF",
   donut = false,
diff --git a/tools/analysis/face_tracker.py b/tools/analysis/face_tracker.py
index f9ed63e..6745a4d 100644
--- a/tools/analysis/face_tracker.py
+++ b/tools/analysis/face_tracker.py
@@ -117,9 +117,14 @@ class FaceTracker(BaseTool):
     def _has_mediapipe(self) -> bool:
         try:
             import mediapipe  # noqa: F401
-            return True
         except ImportError:
             return False
+        # mediapipe 1.0 dropped the legacy mp.solutions API this tool tracks
+        # faces with. Importing successfully is not enough — without it the
+        # OpenCV Haar path is the working one.
+        return hasattr(mediapipe, "solutions") and hasattr(
+            mediapipe.solutions, "face_detection"
+        )
 
     def _has_opencv(self) -> bool:
         try:
diff --git a/tools/video/remotion_caption_burn.py b/tools/video/remotion_caption_burn.py
index 35ff6ba..410fae0 100644
--- a/tools/video/remotion_caption_burn.py
+++ b/tools/video/remotion_caption_burn.py
@@ -112,6 +112,19 @@ class RemotionCaptionBurn(BaseTool):
                 "default": "#22D3EE",
                 "description": "Highlight color for the active word (hex).",
             },
+            "caption_style": {
+                "type": "object",
+                "description": (
+                    "Extra caption styling forwarded to the TalkingHead "
+                    "composition. Recognised keys: captionColor, "
+                    "captionBackgroundColor, captionFontFamily, "
+                    "captionWordSeparator, captionVerticalPosition (0-1 "
+                    "fraction of frame height for the caption's centre), "
+                    "captionTextTransform ('uppercase' | 'lowercase' | "
+                    "'none'), captionTextShadow. Unset keys keep the "
+                    "composition defaults."
+                ),
+            },
             "corrections": {
                 "type": "object",
                 "description": (
@@ -273,6 +286,7 @@ class RemotionCaptionBurn(BaseTool):
         font_size: int,
         highlight_color: str,
         overlays: list[dict] | None = None,
+        caption_style: dict | None = None,
     ) -> ToolResult:
         root = self._find_remotion_root()
         if root is None:
@@ -312,13 +326,18 @@ class RemotionCaptionBurn(BaseTool):
 
         # Build props JSON
         props = {
-            "videoSrc": f"public/talking-head/{video_filename}",
+            # staticFile() resolves relative to public/, and rejects the
+            # "public/" prefix outright — pass the path without it.
+            "videoSrc": f"talking-head/{video_filename}",
             "captions": captions,
             "overlays": overlays or [],
             "wordsPerPage": words_per_page,
             "fontSize": font_size,
             "highlightColor": highlight_color,
         }
+        # Caller-supplied look (position, case, colours) rides alongside the
+        # core props; unknown keys are simply ignored by the composition.
+        props.update(caption_style or {})
         props_dir = root / "public" / "demo-props"
         props_dir.mkdir(parents=True, exist_ok=True)
         props_file = props_dir / f"caption-burn-{Path(input_path).stem}.json"
@@ -448,6 +467,7 @@ class RemotionCaptionBurn(BaseTool):
         words_per_page = inputs.get("words_per_page", 4)
         font_size = inputs.get("font_size", 52)
         highlight_color = inputs.get("highlight_color", "#22D3EE")
+        caption_style = inputs.get("caption_style") or {}
 
         if not Path(input_path).exists():
             return ToolResult(success=False, error=f"Input video not found: {input_path}")
@@ -479,7 +499,7 @@ class RemotionCaptionBurn(BaseTool):
             result = self._render_remotion(
                 input_path, output_path, captions,
                 words_per_page, font_size, highlight_color,
-                overlays=overlays,
+                overlays=overlays, caption_style=caption_style,
             )
         else:
             result = self._render_ffmpeg(input_path, output_path, captions)
